{
  /*Внутри eval подставляеться true - это не работает */
  // TODO: флаг на prepare_ что из eval
  // На какой уровень вложенности тянуть eval
  // Наверное первым ограничеться
  //  или сделать loads? ? да !
  "test":{
    "val1": "{% ${N} % 2 == 0 %}",
    "val2":"${N}-${val1}",
    "val3":"{% ${N}-${val2} %}",
    "example":{"t1":"${val1}", "t2":"${val1} ", "t3":"${val2}", "t4":"{% ${val3} %}"}
  }
}

