#pragma once 
#include <string>

#include <wfc/inet/srv/config.hpp>

namespace wfc{ namespace inet{ namespace srv{  namespace ip{

struct config
  : wfc::inet::srv::config
{
  config()
    : wfc::inet::srv::config()
  {
  }
};

}}}}
